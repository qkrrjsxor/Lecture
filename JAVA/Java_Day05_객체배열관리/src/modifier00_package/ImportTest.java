package modifier00_package;

import java.util.*;
import java.util.logging.Logger; // util 패키지에 있는 모든 클래스 *로 import 했는데 util.logging 패키지는 따로 임포트 해야한다
								// 자바는 사실 하위 패키지라는 개념이 없다. 엄연하게 다른 패키지이다.

public class ImportTest {
    public static void main(String[] args) {
    	Scanner sc = new Scanner(System.in);
    	Date d = new Date();
    	int[] arr = new int[] {1, 2, 3, 4};
    	Arrays.toString(arr);
    	
    	Logger 1;  //
 
        
    }
}
